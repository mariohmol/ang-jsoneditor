name: CI

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "npm"
      - run: npm ci
      - name: Lint
        run: ./node_modules/.bin/ng lint

  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "npm"
      - run: npm ci

      - name: Build library
        run: ./node_modules/.bin/ng build --project ang-jsoneditor

      - name: Build demo app
        run: ./node_modules/.bin/ng build

      - name: Run unit tests
        run: ./node_modules/.bin/ng test --project ang-jsoneditor --watch=false --browsers=ChromeHeadlessNoSandbox
